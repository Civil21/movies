<nav class="navbar navbar-expand-lg navbar-light bg-white text-center">
  <%= link_to "Movies", root_path, class:"navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <%= form_tag search_path,class:"form-inline d-flex align-items-center justify-content-center mt-2 mt-lg-0", method: :get  do%>
      <li class="nav-item">
        <%= text_field_tag :q,params[:q],class:"form-control", placeholder: "Search"%>
        <%= button_tag class:"btn btn-outline-success my-2 my-sm-0 d-none d-md-inline" do %>
          <i class="fas fa-search"></i>
        <%end%>
        </li>
      <%end%>
      <li class="nav-item d-flex align-items-center justify-content-center mt-2 mt-lg-0">
        <%= link_to "Категорії", categories_path, class:"nav-link" %>
      </li>
    </ul>
    <ul class="navbar-nav">
      <% unless user_signed_in? %>
        <li class="nav-item mt-2 mt-lg-0">
          <%= link_to "Вхід", new_user_session_path, class:"nav-link" %>
        </li>
      <%else%>
        <li class="nav-item d-flex align-items-center"><%=link_to current_user.nickname, profile_path , class:"nav-link"%></li>
        <li class="nav-item"><%= link_to "Вийти", destroy_user_session_path,method: :delete, class:"nav-link" %></li>
      <%end%>
    </ul>
  </div>
</nav>
